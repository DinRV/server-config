apiVersion: v1
kind: Service
metadata:
  name: server-config-api
  namespace: platform
  labels:
    app: server-config-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: server-config-api
